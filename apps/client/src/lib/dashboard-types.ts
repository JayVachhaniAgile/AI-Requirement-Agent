/** Re-export shared dashboard DTOs (single source of truth). */
export type {
  DashboardStep,
  DocumentStats,
  ProjectDashboard,
  AgentActivityPhase,
  AgentActivityEvent,
  KnowledgeCreatedEvent,
} from "@workspace/shared-types";
