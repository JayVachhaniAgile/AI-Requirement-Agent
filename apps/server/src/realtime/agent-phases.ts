/** Re-export shared agent phase / display maps. */
export {
  AGENT_PHASES,
  AGENT_DISPLAY_NAMES,
  DEFAULT_STAGE_SECONDS,
  STAGE_TO_AGENT,
  getPhasesForAgent,
  type AgentPhaseDef,
} from '@workspace/pipeline-config';
